name: Web App Deployment Workflow

on:
  pull_request:
    branches: ["main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Simulate build
        run: echo "Building web app (training simulation)"

      - name: Simulate deployment
        run: echo "Deploying web app (training simulation)"
